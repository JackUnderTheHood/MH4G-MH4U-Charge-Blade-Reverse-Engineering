# 发布说明

## 快速变形斩 GP v4

v4 是快速变形斩补丁的最终原生时序实现。

与 v3 相比，v4 不再通过五个 hook 和资源 overlay 替换动作身份。它保留两种快速变形斩原本的 Action 与动画，只在游戏本来就要更新状态的正确时机让 GP 判定生效，并在成功防御后按红盾条件进入原生瓶爆链。

主要变化：

- 五个 hook 减少为两个；
- 640-byte overlay 改为 264-byte cave；
- 不再修改 motion identity；
- 无红盾 GP 与红盾 GP 瓶爆分流明确；
- 不包含 logger、计数器或逐帧写入；
- JPN／USA／EUR 三版均独立完成运行时验收。

v4 会取代旧 v3。更换前请完全关闭 Azahar，并不要读取跨版本即时存档。

## 剑模式蓄力斩 GP v1

这是包含两项功能的合并版：

- 完整包含快速变形斩 GP v4；
- 额外加入剑模式蓄力阶段 GP；
- 小／中后退继续蓄力，大后退正常打断；
- 红盾成功 GP 产生一次原生瓶爆，无红盾不产生瓶爆。

该包使用共享 GP 分派器，不能与独立快速 v4 同时安装。

## 测试边界

普通变形、普通 GP、长按 R 防御、主要派生、蓄力释放、红盾条件和三档后退已经覆盖。怪物多段攻击没有专项覆盖；现有测试未观察到重复瓶爆，但不作穷尽性声明。

完整发布物校验值见 [ARTIFACTS.zh-CN.md](ARTIFACTS.zh-CN.md)。
